// Delete this 1 line if run on browser
var chai = require('chai');

var assert = chai.assert;

describe('Test Title', function() {
  it('should ...', function() {
    var a = 1;

    assert.equal(a, 1, 'a is not 0');
  });

});

